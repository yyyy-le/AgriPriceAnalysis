# 农产品物价数据分析系统 - 项目文件结构说明

## 📁 项目根目录结构

```
AgriPriceAnalysis/
├── fastapi/              # 后端服务（FastAPI + Python）
├── web/                  # 前端服务（Vue 3 + Element Plus）
├── test/                 # 测试脚本和数据
├── doc/                  # 项目文档
├── 数据集/               # 原始数据集
├── .claude/              # Claude Code 配置
├── .vscode/              # VS Code 配置
└── README.md             # 项目说明
```

---

## 🔧 后端服务 (fastapi/)

### 核心文件

| 文件 | 功能 |
|------|------|
| `main.py` | FastAPI 应用入口，启动服务 |
| `api_app.py` | API 应用实例创建 |
| `create_admin.py` | 创建管理员账号脚本 |
| `get_schema.py` | 导出 OpenAPI Schema |

### 目录结构

#### 📂 `app/` - 应用核心代码

##### `app/http/api/` - API 路由模块

| 文件 | 功能 | 主要接口 |
|------|------|---------|
| `auth.py` | 用户认证 | 登录、注册、刷新 Token |
| `user.py` | 用户管理 | 个人信息、修改密码 |
| `admin.py` | 管理员功能 | 数据导入、用户管理、系统日志 |
| `price.py` | 价格查询 | 价格列表、统计、排行榜 |
| `predict.py` | 价格预测 | Prophet 模型预测、AI 分析报告 |
| `ai.py` | AI 助手 | 智能对话、工具调用（价格查询、对比） |
| `alerts.py` | 价格预警 | 预警规则管理、触发记录 |
| `crawl.py` | 数据爬取 | 新发地市场数据爬虫 |
| `boot.py` | 系统启动 | 健康检查、配置信息 |

##### `app/http/deps/` - 依赖注入

| 文件 | 功能 |
|------|------|
| `auth_deps.py` | 认证依赖（获取当前用户、管理员权限） |
| `database_deps.py` | 数据库会话依赖 |
| `firewall_deps.py` | 防火墙/IP 白名单 |
| `request_deps.py` | 请求上下文（时区、客户端 IP） |

##### `app/http/middleware/` - 中间件

| 文件 | 功能 |
|------|------|
| `real_ip.py` | 真实 IP 获取（支持代理） |

##### `app/jobs/` - 定时任务

| 文件 | 功能 |
|------|------|
| `xinfadi_crawler.py` | 新发地市场价格爬虫 |
| `alert_checker.py` | 价格预警检查器 |
| `base_crawler.py` | 爬虫基类 |

##### `app/models/` - 数据库模型

| 文件 | 功能 |
|------|------|
| `user.py` | 用户模型（UserModel） |
| `base_model.py` | 模型基类（时间戳、UUID） |

##### `app/schemas/` - 数据验证模式

| 文件 | 功能 |
|------|------|
| `user.py` | 用户相关 Schema |
| `token.py` | Token Schema |
| `jwt.py` | JWT Payload Schema |
| `oauth2.py` | OAuth2 Schema |
| `common.py` | 通用 Schema（分页、响应） |

##### `app/services/` - 业务逻辑服务

| 目录/文件 | 功能 |
|----------|------|
| `auth/grant_service.py` | OAuth2 授权服务 |
| `auth/token_service.py` | Token 生成/验证 |
| `auth/user_service.py` | 用户业务逻辑 |
| `auth/validation_service.py` | 参数验证服务 |
| `auth/verification_code_service.py` | 验证码服务 |
| `sms/sms_sender.py` | 短信发送服务 |

##### `app/support/` - 工具函数

| 文件 | 功能 |
|------|------|
| `jwt_helper.py` | JWT 编码/解码 |
| `password_helper.py` | 密码哈希/验证 |
| `time_helper.py` | 时间处理 |
| `string_helper.py` | 字符串工具 |
| `path_helper.py` | 路径处理 |
| `csrf_helper.py` | CSRF Token |
| `httpx_helper.py` | HTTP 客户端封装 |

##### `app/providers/` - 服务提供者

| 文件 | 功能 |
|------|------|
| `app_provider.py` | FastAPI 应用创建 |
| `database_provider.py` | 数据库连接池 |
| `route_provider.py` | 路由注册 |
| `middleware_provider.py` | 中间件注册 |
| `exception_provider.py` | 异常处理器 |
| `logging_provider.py` | 日志配置 |
| `lifespan_provider.py` | 应用生命周期 |
| `rate_limiter_provider.py` | 限流器 |

#### 📂 `config/` - 配置文件

| 文件 | 功能 |
|------|------|
| `config.py` | 主配置（环境变量加载） |
| `database.py` | 数据库配置 |
| `auth.py` | 认证配置（JWT 密钥、过期时间） |
| `ai.py` | AI 配置（DeepSeek API） |
| `http.py` | HTTP 配置（CORS、限流） |
| `logging.py` | 日志配置 |
| `redis_key.py` | Redis Key 前缀 |

#### 📂 `bootstrap/` - 启动引导

| 文件 | 功能 |
|------|------|
| `application.py` | 应用启动器 |
| `scheduler.py` | 定时任务调度器 |

#### 📂 `migrations/` - 数据库迁移

| 文件 | 功能 |
|------|------|
| `env.py` | Alembic 环境配置 |
| `versions/0001_init.py` | 初始化数据库表 |
| `versions/0002_admin_logs.py` | 管理员日志表 |
| `versions/ddl/*.sql` | PostgreSQL 扩展安装 |

#### 📂 `storage/` - 存储目录

| 目录 | 功能 |
|------|------|
| `logs/` | 应用日志文件 |

---

## 🎨 前端服务 (web/)

### 核心文件

| 文件 | 功能 |
|------|------|
| `index.html` | HTML 入口 |
| `vite.config.js` | Vite 构建配置 |
| `package.json` | 依赖管理 |

### 目录结构

#### 📂 `src/` - 源代码

##### `src/views/` - 页面组件

| 目录/文件 | 功能 |
|----------|------|
| `user/Home.vue` | 用户首页（数据概览） |
| `user/PriceList.vue` | 价格查询列表 |
| `user/Analysis.vue` | 价格预测分析 |
| `user/AiChat.vue` | AI 智能助手 |
| `user/Profile.vue` | 个人中心 |
| `admin/Dashboard.vue` | 管理员仪表盘 |
| `admin/DataImport.vue` | 数据导入 |
| `admin/UserManage.vue` | 用户管理 |
| `admin/SystemLogs.vue` | 系统日志 |
| `Login.vue` | 登录页 |
| `Register.vue` | 注册页 |

##### `src/api/` - API 请求封装

| 文件 | 功能 |
|------|------|
| `auth.js` | 认证 API（登录、注册） |
| `price.js` | 价格查询 API |
| `predict.js` | 价格预测 API |
| `ai.js` | AI 助手 API |
| `admin.js` | 管理员 API |
| `user.js` | 用户 API |

##### `src/router/` - 路由配置

| 文件 | 功能 |
|------|------|
| `index.js` | Vue Router 配置（路由表、导航守卫） |

##### `src/stores/` - 状态管理

| 文件 | 功能 |
|------|------|
| `user.js` | 用户状态（Pinia Store） |

##### `src/components/` - 公共组件

| 文件 | 功能 |
|------|------|
| `NavBar.vue` | 顶部导航栏 |
| `SideBar.vue` | 侧边栏 |

##### `src/utils/` - 工具函数

| 文件 | 功能 |
|------|------|
| `request.js` | Axios 封装（拦截器、Token 注入） |

##### `src/assets/` - 静态资源

| 目录 | 功能 |
|------|------|
| `css/` | 样式文件 |
| `images/` | 图片资源 |

---

## 🧪 测试目录 (test/)

### 核心文件

| 文件 | 功能 | 技术栈 |
|------|------|--------|
| `prophet_accuracy_test.py` | Prophet 模型精度测试 | Python + Prophet + PostgreSQL |
| `generate_report.js` | Word 测试报告生成器 | Node.js + docx |
| `prophet_accuracy_result.csv` | 测试结果数据 | CSV |

### 目录结构

| 目录 | 功能 |
|------|------|
| `shujuji/` | 原始数据集（CSV 格式） |
| `数据集/` | 原始数据集（Excel 格式） |
| `prophet_results/diagnostics/` | Prophet 诊断图表（HTML） |

### 测试流程

```
1. prophet_accuracy_test.py
   ↓ 从数据库读取 10 个品类数据
   ↓ 训练 Prophet 模型（前 80% 训练，后 20% 测试）
   ↓ 计算 MAE、RMSE、MAPE
   ↓ 输出到 prophet_accuracy_result.csv

2. generate_report.js
   ↓ 读取 CSV 数据
   ↓ 构建 Word 文档（表格、分析、建议）
   ↓ 输出 prophet_accuracy_report.docx
```

---

## 📚 文档目录 (doc/)

| 文件 | 功能 |
|------|------|
| `数据库表结构文档.md` | 数据库表设计说明 |
| `第五章_系统实现.md` | 论文第五章内容 |
| `第六章_系统测试.md` | 论文第六章内容 |
| `generate_chapter4.py` | 自动生成论文第四章脚本 |
| `generate_chapter4_submodules.py` | 生成子模块文档脚本 |

---

## 📊 数据集目录 (数据集/)

存放原始数据集文件（Excel/CSV 格式），用于数据导入和测试。

---

## 🔑 核心技术栈

### 后端
- **框架**: FastAPI 0.115+
- **数据库**: PostgreSQL 14+
- **ORM**: SQLAlchemy 2.0 (Async)
- **认证**: OAuth2 + JWT
- **预测**: Prophet (Facebook)
- **AI**: DeepSeek API (流式输出)
- **爬虫**: httpx + BeautifulSoup
- **定时任务**: APScheduler

### 前端
- **框架**: Vue 3 + Vite
- **UI 库**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP**: Axios
- **图表**: ECharts

### 测试
- **精度测试**: Python + Prophet + pandas
- **报告生成**: Node.js + docx

---

## 🚀 启动命令

### 后端
```bash
cd fastapi
python main.py
# 访问 http://localhost:8000/docs
```

### 前端
```bash
cd web
npm install
npm run dev
# 访问 http://localhost:5173
```

### 测试
```bash
# Prophet 精度测试
python test/prophet_accuracy_test.py

# 生成 Word 报告
cd test
npm install docx
node generate_report.js
```

---

## 📝 数据库表结构

| 表名 | 功能 |
|------|------|
| `users` | 用户表 |
| `categories` | 产品分类表（支持二级分类） |
| `products` | 产品表 |
| `markets` | 市场表 |
| `price_records` | 价格记录表 |
| `price_alerts` | 价格预警规则表 |
| `alert_logs` | 预警触发日志表 |
| `admin_logs` | 管理员操作日志表 |

---

## 🔐 环境变量配置

在 `fastapi/.env` 中配置：

```env
# 数据库
DATABASE_URL=postgresql+asyncpg://postgres:123456@localhost:5432/agriprice

# JWT
JWT_SECRET_KEY=your-secret-key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# DeepSeek API
DEEPSEEK_API_KEY=your-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
```

---

## 📖 论文对应关系

| 论文章节 | 对应代码 |
|---------|---------|
| 第四章 系统设计 | `doc/generate_chapter4.py` 自动生成 |
| 第五章 系统实现 | `fastapi/app/` 全部代码 + `web/src/` 全部代码 |
| 第六章 系统测试 | `test/prophet_accuracy_test.py` + `test/generate_report.js` |
| 表 6-7 接口响应时间 | JMeter 测试（需手动配置） |
| 表 6-8 并发测试 | JMeter 测试（需手动配置） |

---

**生成时间**: 2026-05-07  
**项目版本**: 1.0.0
