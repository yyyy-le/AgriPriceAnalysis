# 农产品物价数据分析系统 - 前端详细文档

## 📁 项目结构

```
web/
├── index.html              # HTML 入口文件
├── vite.config.js          # Vite 构建配置
├── package.json            # 项目依赖管理
├── .env                    # 环境变量配置
├── public/                 # 静态资源目录
│   └── favicon.ico         # 网站图标
└── src/                    # 源代码目录
    ├── main.js             # Vue 应用入口
    ├── App.vue             # 根组件
    ├── style.css           # 全局样式
    ├── views/              # 页面组件
    │   ├── Login.vue       # 登录页
    │   ├── Register.vue    # 注册页
    │   ├── Layout.vue      # 用户端布局
    │   ├── AdminLayout.vue # 管理端布局
    │   ├── user/           # 用户端页面
    │   │   ├── Home.vue           # 首页（数据概览）
    │   │   ├── PriceList.vue      # 价格查询
    │   │   ├── Analysis.vue       # 价格预测
    │   │   ├── AiChat.vue         # AI 助手
    │   │   └── Profile.vue        # 个人中心
    │   └── admin/          # 管理端页面
    │       ├── Dashboard.vue      # 管理员仪表盘
    │       ├── DataImport.vue     # 数据导入
    │       ├── UserManage.vue     # 用户管理
    │       └── SystemLogs.vue     # 系统日志
    ├── components/         # 公共组件
    │   ├── NavBar.vue      # 顶部导航栏
    │   └── SideBar.vue     # 侧边栏菜单
    ├── api/                # API 请求封装
    │   ├── auth.js         # 认证相关 API
    │   ├── price.js        # 价格查询 API
    │   ├── predict.js      # 价格预测 API
    │   ├── ai.js           # AI 助手 API
    │   ├── admin.js        # 管理员 API
    │   └── user.js         # 用户 API
    ├── router/             # 路由配置
    │   └── index.js        # Vue Router 配置
    ├── stores/             # 状态管理
    │   └── user.js         # 用户状态 Store
    ├── utils/              # 工具函数
    │   ├── request.js      # Axios 封装
    │   └── format.js       # 格式化工具
    └── assets/             # 静态资源
        ├── logo.png        # Logo 图片
        └── styles/         # 样式文件
```

---

## 一、根目录配置文件

### 1. `package.json` - 项目依赖管理

**功能**: 定义项目依赖、脚本命令、项目信息

**完整内容**:
```json
{
  "name": "agriprice-web",
  "version": "1.0.0",
  "description": "农产品物价数据分析系统 - 前端",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0",
    "pinia": "^2.1.0",
    "element-plus": "^2.5.0",
    "axios": "^1.6.0",
    "echarts": "^5.4.0",
    "@element-plus/icons-vue": "^2.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
```

**依赖说明**:
| 依赖 | 版本 | 用途 |
|------|------|------|
| vue | ^3.4.0 | Vue 3 核心框架 |
| vue-router | ^4.2.0 | 路由管理 |
| pinia | ^2.1.0 | 状态管理（替代 Vuex） |
| element-plus | ^2.5.0 | UI 组件库 |
| axios | ^1.6.0 | HTTP 请求库 |
| echarts | ^5.4.0 | 图表库 |
| @element-plus/icons-vue | ^2.3.0 | Element Plus 图标 |
| vite | ^5.0.0 | 构建工具 |

**脚本命令**:
```bash
npm run dev      # 启动开发服务器（http://localhost:5173）
npm run build    # 构建生产版本
npm run preview  # 预览生产构建
```

---

### 2. `vite.config.js` - Vite 构建配置

**功能**: 配置开发服务器、构建选项、插件、代理

**完整内容**:
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  // 插件配置
  plugins: [
    vue()
  ],
  
  // 路径别名
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  },
  
  // 开发服务器配置
  server: {
    port: 5173,
    host: '0.0.0.0',
    open: true,  // 自动打开浏览器
    
    // API 代理配置（解决跨域）
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
        rewrite: (path) => path
      }
    }
  },
  
  // 构建配置
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,
    
    // 代码分割
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'element-plus': ['element-plus'],
          'echarts': ['echarts']
        }
      }
    }
  }
})
```

**配置说明**:
- **路径别名**: `@` 指向 `src/` 目录，可以用 `@/api/price.js` 代替 `../../api/price.js`
- **开发服务器**: 端口 5173，自动打开浏览器
- **API 代理**: 将 `/api` 请求代理到 `http://localhost:8000`，解决跨域问题
- **代码分割**: 将 Vue、Element Plus、ECharts 分别打包，优化加载速度

---

### 3. `index.html` - HTML 入口文件

**功能**: 应用的 HTML 模板

**完整内容**:
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="农产品物价数据分析系统">
  <meta name="keywords" content="农产品,价格,预测,分析">
  <link rel="icon" href="/favicon.ico">
  <title>农产品物价数据分析系统</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/main.js"></script>
</body>
</html>
```

**说明**:
- `<div id="app"></div>`: Vue 应用挂载点
- `<script type="module" src="/src/main.js"></script>`: 加载 Vue 应用入口

---

### 4. `.env` - 环境变量配置

**功能**: 配置环境变量（API 地址等）

**内容**:
```env
# API 基础地址
VITE_API_BASE_URL=http://localhost:8000

# 应用标题
VITE_APP_TITLE=农产品物价数据分析系统
```

**使用方式**:
```javascript
// 在代码中访问环境变量
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL
```

---

## 二、src/ - 源代码目录

### 1. `main.js` - Vue 应用入口

**功能**: 创建 Vue 应用实例，注册插件

**完整内容**:
```javascript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'
import App from './App.vue'
import router from './router'
import './style.css'

// 创建 Vue 应用实例
const app = createApp(App)

// 注册 Pinia 状态管理
app.use(createPinia())

// 注册 Vue Router 路由
app.use(router)

// 注册 Element Plus UI 库
app.use(ElementPlus)

// 注册 Element Plus 图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

// 挂载应用到 #app
app.mount('#app')
```

**执行流程**:
1. 创建 Vue 应用实例
2. 注册 Pinia（状态管理）
3. 注册 Vue Router（路由）
4. 注册 Element Plus（UI 组件库）
5. 注册所有 Element Plus 图标
6. 挂载到 DOM

---

// __CONTINUE_HERE__
